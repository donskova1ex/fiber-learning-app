package layout

templ Header() {
    @HeaderStyle()
	<div class="header">
	    @Menu()
	    { children... }
	</div>
}
templ HeaderStyle() {
    <style>
        .header {
            background: #222222;
            display: flex;
            justify-contetnt: center;
            flex-direction: column;
            align-items: center;
        }
    </style>
}